I want you to set up the **MySQL database** for the website. Here are the database credentials:

* **Host:** `mysql-scorasql.alwaysdata.net`
* **Database:** `scorasql_1`
* **Username:** `scorasql`
* **Password:** `S123S123s123`

The database should handle **everything on the website**, including users, developers, and all other data.

Also, remove anything that makes the website look like a prototype. For example:

* Remove the **“تسجيل كمطور”** option.
* Remove the **dropdown menu that contains “Template.”**
* Make sure the website feels like a complete production-ready platform rather than a prototype.

There are also **images being uploaded** and many other features already present on the website. Please review everything that exists and **properly design and configure the database schema to support all of it**, including users, developers, uploaded images, and any other relevant data or relationships.

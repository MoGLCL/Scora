import{notFound}from"next/navigation";import{query,queryOne}from"@/lib/db";import{AdmissionDecisionForm}from"@/components/admission-decision-form";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await queryOne<{id:number;public_id:string;status:string;model:string|null;display_name:string;email:string;username:string|null}>(`SELECT das.id,das.public_id,das.status,das.model,d.display_name,u.email,u.username FROM developer_assessment_sessions das JOIN developers d ON d.id=das.developer_id JOIN users u ON u.id=d.user_id WHERE das.public_id=?`,[id]);if(!s)notFound();const qs=await query<{id:number;kind:string;skill:string;question_text:string;answer_text:string|null;score:number|null}>(`SELECT q.id,q.kind,q.skill,q.question_text,a.answer_text,a.score FROM developer_assessment_questions q LEFT JOIN developer_assessment_answers a ON a.question_id=q.id WHERE q.session_id=? ORDER BY q.position`,[s.id]);const events=await query<{event_type:string;source:string;created_at:Date;event_hash:string}>("SELECT event_type,source,created_at,event_hash FROM trust_events WHERE session_public_id=? ORDER BY chain_position",[id]);return <main dir="rtl" className="mx-auto max-w-5xl space-y-6 px-6 py-10"><div><a href="/admin" className="text-[#056B38]">← لوحة الإدارة</a><h1 className="mt-3 text-3xl font-extrabold">مراجعة {s.display_name}</h1><p className="text-gray-600">@{s.username} · {s.email} · النموذج: {s.model||"غير مسجل"}</p></div>{qs.map((q,i)=><article key={q.id} className="rounded-2xl border bg-white p-6"><div className="text-xs font-bold text-[#056B38]">{q.kind.toUpperCase()} · {q.skill}</div><h2 className="mt-2 font-extrabold">{i+1}. {q.question_text}</h2><pre className="mt-4 whitespace-pre-wrap rounded-xl bg-gray-50 p-4">{q.answer_text||"لم تتم الإجابة"}</pre>{q.score!==null&&<p className="mt-2">الدرجة: {q.score}</p>}</article>)}<section className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-extrabold">سجل Trust Engine ({events.length})</h2><div className="mt-4 space-y-2">{events.map((e,i)=><div key={i} className="rounded-xl bg-gray-50 p-3 text-sm"><b>{e.event_type}</b> · {e.source}<div className="truncate font-mono text-[10px]">{e.event_hash}</div></div>)}</div></section>{s.status==="admin_review"&&<AdmissionDecisionForm assessmentPublicId={s.public_id}/>}</main>}

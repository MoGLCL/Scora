import {redirect} from "next/navigation";import {verifySession} from "@/lib/dal";export default async function Page(){const s=await verifySession();if(!s)redirect('/login');return <main dir="rtl" className="mx-auto max-w-2xl px-6 py-24 text-center"><h1 className="text-3xl font-extrabold">اختبارك عند الأدمن دلوقتي</h1><p className="mt-4 text-gray-600">أول ما تتم المراجعة هيوصلك إشعار، ولحد وقتها حساب المطور مش هيدخل لباقي المنصة.</p></main>}

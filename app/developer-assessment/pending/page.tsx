import{redirect}from"next/navigation";import{verifySession}from"@/lib/dal";import{AdmissionStatus}from"@/components/admission-status";
export default async function Page(){const session=await verifySession();if(!session)redirect("/login");if(session.role!=="developer")redirect("/dashboard");if(session.developerApprovalStatus==="approved")redirect("/dashboard");return <main dir="rtl" className="mx-auto max-w-2xl px-6 py-24 text-center"><h1 className="text-3xl font-extrabold">حالة اعتماد المطور</h1><AdmissionStatus/></main>}

import{NextResponse}from"next/server";import{verifySession}from"@/lib/dal";import{openRouterConfig}from"@/lib/openrouter";export async function GET(){const s=await verifySession();if(!s?.isAdmin)return NextResponse.json({error:"FORBIDDEN"},{status:403});const c=await openRouterConfig();return NextResponse.json({model:c.model,siteUrl:c.siteUrl,siteTitle:c.siteTitle,hasApiKey:Boolean(c.apiKey)})}
